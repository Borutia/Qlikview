Directory; //Создается автоматически, если использован опция Относительные пути. Через пробел можно задать папку, где будет смотреться файлы.

tmp:
LOAD Distinct
	 A, 
     B, 
     C
     //FileName() as Имя_файла
FROM
[Каталог\q2018_??*.xls] //Для загрузки по шаблону можно использовать *,?
(biff, embedded labels, table is Лист3$);